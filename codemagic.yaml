workflows:
  default-workflow:
    name: Build KamiChat APK
    environment:
      flutter: stable
    scripts:
      - name: Get Dependencies
        script: flutter pub get
      - name: Build APK
        script: flutter build apk --debug
    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk
